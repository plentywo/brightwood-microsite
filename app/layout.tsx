import React from 'react'
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Brightwood Health - Transform Your Health Journey',
  description: 'Discover personalized health solutions and expert guidance to transform your wellness journey with Brightwood Health.',
  keywords: 'health, wellness, personalized care, medical services, Brightwood Health',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
} 